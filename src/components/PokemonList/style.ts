import styled from 'styled-components';

export const PokemonListContainer = styled.article`
    display: flex;
    flex-wrap: wrap;

    max-width: 60vw;
    justify-content: center;
    margin: auto;

    border: solid blue 1px;
`;